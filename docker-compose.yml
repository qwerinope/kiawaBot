version: '3.8'

services:
  kiara-bot:
    build: .
    container_name: kiara-bot
    restart: unless-stopped
    ports:
      - "3000:3000"  # OAuth redirect port
      - "8081:8081"  # Web server port
    environment:
      - NODE_ENV=production
      - WEB_PORT=8081
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - bot_logs:/app/logs
    networks:
      - bot-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  bot_logs:
    driver: local

networks:
  bot-network:
    driver: bridge